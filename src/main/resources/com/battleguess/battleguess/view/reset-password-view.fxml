<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<StackPane xmlns:fx="http://javafx.com/fxml" fx:controller="com.battleguess.battleguess.controller.ResetPasswordController">
    <VBox alignment="CENTER" spacing="20" maxWidth="450"
          style="-fx-background-color: linear-gradient(to bottom right, #2b5876, #4e4376); -fx-padding: 40; -fx-background-radius: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.25), 10,0,0,4)">

        <Label text="🔐 Đặt lại mật khẩu" style="-fx-font-size: 22; -fx-text-fill: white; -fx-font-weight: bold;" />

        <HBox spacing="10" alignment="CENTER_LEFT" maxWidth="Infinity">
            <Label text="Port:" style="-fx-text-fill: white; -fx-min-width: 100;" />
            <TextField fx:id="txtPort" promptText="Cổng Server (VD: 2222)" style="-fx-background-radius: 10; -fx-padding: 8;" HBox.hgrow="ALWAYS"/>
        </HBox>

        <HBox spacing="10" alignment="CENTER_LEFT" maxWidth="Infinity">
            <Label text="Tên tài khoản:" style="-fx-text-fill: white; -fx-min-width: 100;" />
            <TextField fx:id="txtUsername" promptText="Nhập tên đăng nhập" style="-fx-background-radius: 10; -fx-padding: 8;" HBox.hgrow="ALWAYS"/>
        </HBox>

        <HBox spacing="10" alignment="CENTER_LEFT" maxWidth="Infinity">
            <Label text="Mật khẩu mới:" style="-fx-text-fill: white; -fx-min-width: 100;" />
            <PasswordField fx:id="txtNewPassword" promptText="Nhập mật khẩu mới" style="-fx-background-radius: 10; -fx-padding: 8;" HBox.hgrow="ALWAYS"/>
        </HBox>

        <HBox spacing="10" alignment="CENTER_LEFT" maxWidth="Infinity">
            <Label text="Xác nhận:" style="-fx-text-fill: white; -fx-min-width: 100;" />
            <PasswordField fx:id="txtConfirmPassword" promptText="Nhập lại mật khẩu mới" style="-fx-background-radius: 10; -fx-padding: 8;" HBox.hgrow="ALWAYS"/>
        </HBox>

        <HBox spacing="20" alignment="CENTER" maxWidth="Infinity">
            <Button text="Quay lại" onAction="#onBackClicked"
                    style="-fx-background-color: transparent; -fx-text-fill: #bdc3c7; -fx-border-color: #bdc3c7; -fx-border-radius: 10; -fx-background-radius: 10;"/>

            <Button text="Xác nhận đổi" onAction="#onResetClicked"
                    style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-padding: 8 16;"/>
        </HBox>

        <Label fx:id="lblStatus" text="" wrapText="true" style="-fx-text-fill: #ffdddd; -fx-font-size: 12;" alignment="CENTER"/>
    </VBox>
</StackPane>